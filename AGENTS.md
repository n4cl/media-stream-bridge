# エージェント向けルール

## 開発

変更へ着手する前に `docs/development.md` と `docs/rules/change.md` を読む。

変更目的、契約、テスト、ADRなどの保存先や更新先を判断するときは、`docs/rules/information-lifecycle.md` に従う。

## コミット

コミットを作成する直前に `docs/rules/commit-message.md` を読み、そのルールに従う。
